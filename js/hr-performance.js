import { abrirPagina, admin } from './core.js';
import { $, esc, msg, permite, state, listarDocumentos, criarDocumento, atualizarDocumento, empresaUnicaSelecionadaId, emitirAlteracao } from './shared.js';
import { confirmarAcaoAdministrativa, atualizarComAuditoria } from './admin-actions.js';
import { periodoAtual } from './company-context.js';
import { fimMes } from './hr-metrics.js';

const COMP=['Colaboração','Entrega','Comunicação','Iniciativa','Liderança'];
const MODELOS={
  '90':{rotulo:'90°',papeis:['LIDERANÇA'],descricao:'Gestor direto'},
  '180':{rotulo:'180°',papeis:['LIDERANÇA','AUTOAVALIAÇÃO'],descricao:'Gestor + autoavaliação'},
  '360':{rotulo:'360°',papeis:['LIDERANÇA','AUTOAVALIAÇÃO','PAR','LIDERADO'],descricao:'Gestor + autoavaliação + pares + liderados'},
  '540':{rotulo:'540°',papeis:['LIDERANÇA','AUTOAVALIAÇÃO','PAR','LIDERADO','CLIENTE','FORNECEDOR'],descricao:'360° + clientes + fornecedores'}
};
const PAPEIS=['AUTOAVALIAÇÃO','LIDERANÇA','PAR','LIDERADO','CLIENTE','FORNECEDOR'];
const EXTERNOS=['CLIENTE','FORNECEDOR'];
const ABAS_AV=['avaliacoes','feedbacks','pdis','calibracao','historico'];

let registros={avaliacoes:[],acoes:[]},pessoas=[],abaAv='avaliacoes',editId='',busy=false;
const perm=(k,a)=>admin()||permite(k==='acoes'?'rhAcoes':'rhAvaliacoes360',a);
const podeVer=k=>['visualizar','lancar','editar'].some(a=>perm(k,a));
const acesso=()=>admin()||permite('rhColaboradores','visualizar')||permite('rhColaboradores','lancar')||permite('rhColaboradores','editar');
const hoje=()=>{const d=new Date();return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`};
const pessoa=id=>pessoas.find(p=>p.id===id)?.nome||'Colaborador indisponível';
const colaborador=id=>pessoas.find(p=>p.id===id);
const modelo=x=>String(x?.modelo||'360');
const nomeAvaliador=x=>EXTERNOS.includes(x?.papel)?(x?.avaliadorExternoNome||'Avaliador externo'):pessoa(x?.avaliadorId);
const mediaNotas=x=>Array.isArray(x?.notas)&&x.notas.length?x.notas.reduce((a,b)=>a+Number(b||0),0)/x.notas.length:0;
const potencialNotas=x=>Array.isArray(x?.notas)&&x.notas.length>=5?(Number(x.notas[3]||0)+Number(x.notas[4]||0))/2:mediaNotas(x);
const dataBR=s=>s?String(s).slice(0,10).split('-').reverse().join('/'):'—';
const prefixo=(tipo,ciclo)=>`[${tipo}:${ciclo}]`;
const cicloAcao=x=>{const m=String(x?.titulo||'').match(/^\[(FEEDBACK|PDI):(\d{4}-\d{2})\]\s*/);return m?.[2]||x?.competencia||''};
const tipoDesenvolvimento=x=>{const m=String(x?.titulo||'').match(/^\[(FEEDBACK|PDI):/);return m?.[1]||''};
const tituloLimpo=x=>String(x?.titulo||'').replace(/^\[(FEEDBACK|PDI):\d{4}-\d{2}\]\s*/,'');

function intervaloAval(){
  const p=periodoAtual(),meses=p.indices.map(i=>`${p.ano}-${String(i+1).padStart(2,'0')}`),inicio=meses[0],fim=meses.at(-1);
  return {...p,inicio,fim,dataInicio:`${inicio}-01`,dataFim:fimMes(fim),valido:true};
}
function dentroPeriodoAvaliacao(x,p){const mes=String(x.ciclo||'');return mes>=p.inicio&&mes<=p.fim}
function dentroPeriodoAcao(x,p){const d=String(x.prazo||`${x.competencia||''}-01`);return d>=p.dataInicio&&d<=p.dataFim}
function filtrosAval(){return{setor:$('rhCultSetor')?.value||'',pessoa:$('rhCultPessoa')?.value||''}}
function pessoaPassa(id,f){const p=colaborador(id);return !!p&&(!f.setor||p.setor===f.setor)&&(!f.pessoa||id===f.pessoa)}
function opcoes(selecionado=''){return '<option value="">Selecione...</option>'+pessoas.filter(x=>x.status!=='estornado').sort((a,b)=>a.nome.localeCompare(b.nome,'pt-BR')).map(p=>`<option value="${esc(p.id)}" ${selecionado===p.id?'selected':''}>${esc(p.nome)} · ${esc(p.codigo)}</option>`).join('')}
function atualizarFiltrosAval(){const setorAtual=$('rhCultSetor')?.value||'',pessoaAtual=$('rhCultPessoa')?.value||'';const setores=[...new Set(pessoas.filter(p=>p.status!=='estornado').map(p=>p.setor).filter(Boolean))].sort((a,b)=>a.localeCompare(b,'pt-BR'));if($('rhCultSetor')){$('rhCultSetor').innerHTML='<option value="">Todos os setores</option>'+setores.map(s=>`<option>${esc(s)}</option>`).join('');$('rhCultSetor').value=setores.includes(setorAtual)?setorAtual:''}const setor=$('rhCultSetor')?.value||'';const lista=pessoas.filter(p=>p.status!=='estornado'&&(!setor||p.setor===setor)).sort((a,b)=>a.nome.localeCompare(b.nome,'pt-BR'));if($('rhCultPessoa')){$('rhCultPessoa').innerHTML='<option value="">Todos</option>'+lista.map(p=>`<option value="${esc(p.id)}">${esc(p.nome)} · ${esc(p.codigo)}</option>`).join('');$('rhCultPessoa').value=lista.some(p=>p.id===pessoaAtual)?pessoaAtual:''}}

function montar(){
  const main=document.querySelector('main.conteudo');if(!main)return;
  if(!$('pagina-rh-avaliacoes')){
    const s=document.createElement('section');s.id='pagina-rh-avaliacoes';s.className='pagina hidden production-page';
    s.innerHTML=`<div class="rh-module-head rh-performance-head"><div class="pagina-cabecalho production-head"><div><span class="eyebrow">RH · DESENVOLVIMENTO</span><h2>Avaliações/feedback</h2><p>Avaliações 90°, 180°, 360° e 540°, feedbacks, PDIs e calibração em um histórico único.</p></div><div class="acoes-cabecalho"><button id="rhCultNovo-avaliacoes" type="button" class="btn-primario">+ Nova avaliação</button><button id="rhCultAtualizar-avaliacoes" type="button" class="btn-secundario">Atualizar</button></div></div><div class="fleet-tabs rh-performance-tabs"><button class="fleet-tab ativo" data-rh-av-tab="avaliacoes" type="button">Avaliações</button><button class="fleet-tab" data-rh-av-tab="feedbacks" type="button">Feedbacks</button><button class="fleet-tab" data-rh-av-tab="pdis" type="button">PDIs</button><button class="fleet-tab" data-rh-av-tab="calibracao" type="button">Calibração 9-box</button><button class="fleet-tab" data-rh-av-tab="historico" type="button">Histórico</button></div></div><div id="rhCultAviso-avaliacoes" class="modulo-aviso hidden"></div><section class="lista-card"><div class="lista-cabecalho production-toolbar"><div><h3>Filtros da avaliação</h3><p>O período segue automaticamente o exercício e a competência selecionados no cabeçalho do SIG.</p></div><div class="rh-filter-bar"><label class="rh-filter-field" for="rhCultSetor">Setor <select id="rhCultSetor"><option value="">Todos os setores</option></select></label><label class="rh-filter-field" for="rhCultPessoa">Colaborador <select id="rhCultPessoa"><option value="">Todos</option></select></label></div></div></section><div id="rhCultResumo-avaliacoes" class="production-kpis"></div><section id="rhCultBox-avaliacoes" class="form-card hidden"><div class="form-card-titulo"><h3 id="rhCultTitulo-avaliacoes">Nova avaliação</h3></div><form id="rhCultForm-avaliacoes"><div class="form-grid form-grid-3" id="rhCultCampos-avaliacoes"></div><div class="form-acoes"><button type="button" class="btn-secundario" id="rhCultCancelar-avaliacoes">Cancelar</button><button type="submit" class="btn-primario">Salvar avaliação</button></div><p id="rhCultMsg-avaliacoes" class="mensagem-form"></p></form></section><section id="rhDevBox" class="form-card hidden"><div class="form-card-titulo"><div><h3 id="rhDevTitulo">Registrar feedback</h3><p id="rhDevNota">Conectado ao ciclo de desempenho selecionado.</p></div></div><form id="rhDevForm"><div id="rhDevCampos" class="form-grid form-grid-3"></div><div class="form-acoes"><button type="button" class="btn-secundario" id="rhDevCancelar">Cancelar</button><button type="submit" class="btn-primario">Salvar</button></div><p id="rhDevMsg" class="mensagem-form"></p></form></section><section class="lista-card rh-performance-panel"><div class="lista-cabecalho"><div><h3 id="rhCultTituloLista">Avaliações do período</h3><p id="rhCultNotaLista">Ciclos, perspectivas e devolutivas.</p></div><div id="rhCultAcoesLista" class="acoes-cabecalho"></div></div><div id="rhCultConteudo-avaliacoes"></div></section>`;
    main.appendChild(s);
    s.querySelectorAll('[data-rh-av-tab]').forEach(b=>b.addEventListener('click',()=>trocarAbaAval(b.dataset.rhAvTab)));
    $('rhCultPessoa').addEventListener('change',renderAvaliacoes);
    $('rhCultSetor').addEventListener('change',()=>{atualizarFiltrosAval();renderAvaliacoes()});
    $('rhCultNovo-avaliacoes').addEventListener('click',()=>abrirAvaliacao());
    $('rhCultAtualizar-avaliacoes').addEventListener('click',()=>carregar());
    $('rhCultCancelar-avaliacoes').addEventListener('click',()=>{$('rhCultBox-avaliacoes').classList.add('hidden');editId=''});
    $('rhCultForm-avaliacoes').addEventListener('submit',salvarAvaliacao);
    $('rhDevCancelar').addEventListener('click',()=>{$('rhDevBox').classList.add('hidden');$('rhDevMsg').textContent=''});
    $('rhDevForm').addEventListener('submit',salvarDesenvolvimento);
  }
}
function menu(){const nav=document.querySelector('.sidebar-menu');if(!nav)return;let b=$('menuRhAvaliacoes');if(!b){b=document.createElement('button');b.id='menuRhAvaliacoes';b.className='menu-item hidden';b.dataset.pagina='rh-avaliacoes';b.type='button';nav.appendChild(b);b.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();if(!acesso()||!podeVer('avaliacoes'))return;abrirPagina('rh-avaliacoes');carregar()},true)}b.textContent='Avaliações/feedback';b.classList.toggle('hidden',!acesso()||!podeVer('avaliacoes'));$('rhCultNovo-avaliacoes')?.classList.toggle('hidden',!perm('avaliacoes','lancar'))}

function camposAvaliacao(x){
  const p=intervaloAval(),ciclo=x?.ciclo||p.fim,mod=modelo(x),papel=x?.papel||MODELOS[mod].papeis[0];
  return `<div class="campo"><label for="cultModelo">Modelo</label><select id="cultModelo">${Object.entries(MODELOS).map(([k,v])=>`<option value="${k}" ${mod===k?'selected':''}>${v.rotulo} · ${v.descricao}</option>`).join('')}</select></div><div class="campo"><label for="cultCiclo">Ciclo (mês)</label><input id="cultCiclo" type="month" required value="${esc(ciclo)}"></div><div class="campo"><label for="cultAvaliado">Avaliado</label><select id="cultAvaliado" required>${opcoes(x?.avaliadoId)}</select></div><div class="campo"><label for="cultPapel">Perspectiva</label><select id="cultPapel">${PAPEIS.map(v=>`<option value="${v}" ${papel===v?'selected':''}>${v}</option>`).join('')}</select></div><div class="campo" id="cultAvaliadorInternoBox"><label for="cultAvaliador">Avaliador interno</label><select id="cultAvaliador">${opcoes(x?.avaliadorId)}</select></div><div class="campo" id="cultAvaliadorExternoBox"><label for="cultAvaliadorExterno">Nome do avaliador externo</label><input id="cultAvaliadorExterno" maxlength="120" value="${esc(x?.avaliadorExternoNome||'')}" placeholder="Cliente ou fornecedor"></div>${COMP.map((v,i)=>`<div class="campo"><label for="cultNota${i}">${v} (1 a 5)</label><input id="cultNota${i}" type="number" min="1" max="5" step="1" required value="${x?.notas?.[i]??''}"></div>`).join('')}<div class="campo campo-span-3"><label for="cultComentario">Evidências e devolutiva</label><textarea id="cultComentario" maxlength="1000" rows="3">${esc(x?.comentario||'')}</textarea></div>`;
}
function ajustarAvaliador(){const mod=$('cultModelo')?.value||'360',papel=$('cultPapel')?.value||'LIDERANÇA',permitidos=MODELOS[mod]?.papeis||[];[...$('cultPapel').options].forEach(o=>o.disabled=!permitidos.includes(o.value));if(!permitidos.includes(papel))$('cultPapel').value=permitidos[0];const ext=EXTERNOS.includes($('cultPapel').value);$('cultAvaliadorInternoBox')?.classList.toggle('hidden',ext);$('cultAvaliadorExternoBox')?.classList.toggle('hidden',!ext);if($('cultAvaliador'))$('cultAvaliador').required=!ext;if($('cultAvaliadorExterno'))$('cultAvaliadorExterno').required=ext}
function abrirAvaliacao(x=null){if(!perm('avaliacoes',x?'editar':'lancar'))return;if(!empresaUnicaSelecionadaId())return alert('Selecione uma empresa no cabeçalho.');editId=x?.id||'';$('rhCultCampos-avaliacoes').innerHTML=camposAvaliacao(x);$('cultModelo').addEventListener('change',ajustarAvaliador);$('cultPapel').addEventListener('change',ajustarAvaliador);ajustarAvaliador();$('rhCultTitulo-avaliacoes').textContent=(x?'Editar':'Nova')+' avaliação de desempenho';msg($('rhCultMsg-avaliacoes'),'');$('rhCultBox-avaliacoes').classList.remove('hidden');$('rhCultBox-avaliacoes').scrollIntoView({behavior:'smooth',block:'start'})}
function lerAvaliacao(){const mod=$('cultModelo').value,ciclo=$('cultCiclo').value,avaliadoId=$('cultAvaliado').value,papel=$('cultPapel').value,externo=EXTERNOS.includes(papel),avaliadorId=externo?'':$('cultAvaliador').value,avaliadorExternoNome=externo?$('cultAvaliadorExterno').value.trim():'',notas=COMP.map((_,i)=>Number($('cultNota'+i).value)),comentario=$('cultComentario').value.trim(),emp=empresaUnicaSelecionadaId();if(!MODELOS[mod]||!MODELOS[mod].papeis.includes(papel)||!/^\d{4}-\d{2}$/.test(ciclo)||!pessoas.some(p=>p.id===avaliadoId&&p.empresaId===emp)||notas.some(n=>!Number.isInteger(n)||n<1||n>5))throw Error('Revise modelo, ciclo, avaliado, perspectiva e notas.');if(externo){if(!avaliadorExternoNome)throw Error('Informe o nome do cliente ou fornecedor que realizou a avaliação.')}else if(!pessoas.some(p=>p.id===avaliadorId&&p.empresaId===emp))throw Error('Selecione um avaliador interno válido.');if((papel==='AUTOAVALIAÇÃO')!==(avaliadoId===avaliadorId))throw Error('Na autoavaliação, avaliado e avaliador devem ser a mesma pessoa.');const chave=externo?avaliadorExternoNome.toLocaleLowerCase('pt-BR'):avaliadorId;if(registros.avaliacoes.some(v=>v.id!==editId&&v.status==='ativo'&&v.ciclo===ciclo&&v.avaliadoId===avaliadoId&&v.papel===papel&&(EXTERNOS.includes(v.papel)?String(v.avaliadorExternoNome||'').toLocaleLowerCase('pt-BR'):v.avaliadorId)===chave))throw Error('Este avaliador já registrou avaliação para esse colaborador no ciclo.');return{modelo:mod,ciclo,avaliadoId,avaliadorId,avaliadorExternoNome,papel,notas,comentario}}
async function salvarAvaliacao(e){e.preventDefault();if(busy)return;try{const emp=empresaUnicaSelecionadaId(),d=lerAvaliacao();busy=true;msg($('rhCultMsg-avaliacoes'),'Salvando...');if(editId){const x=registros.avaliacoes.find(v=>v.id===editId);if(!x||x.empresaId!==emp||x.status!=='ativo'||!perm('avaliacoes','editar'))throw Error('Edição não autorizada.');await atualizarDocumento('rhAvaliacoes360',editId,d)}else{if(!perm('avaliacoes','lancar'))throw Error('Sem permissão.');await criarDocumento('rhAvaliacoes360',{...d,empresaId:emp,status:'ativo',registradoPor:state.usuario.id,origem:'sig'})}$('rhCultBox-avaliacoes').classList.add('hidden');editId='';emitirAlteracao('rh')}catch(err){console.error(err);msg($('rhCultMsg-avaliacoes'),err.message||'Não foi possível salvar.')}finally{busy=false}setTimeout(()=>carregar(),120)}

function abrirDesenvolvimento(tipo,avaliadoId='',ciclo='',sugestao=''){if(!perm('acoes','lancar'))return alert('Seu perfil precisa de permissão para registrar Ações de RH.');if(!empresaUnicaSelecionadaId())return alert('Selecione uma empresa no cabeçalho.');const isPdi=tipo==='PDI',cicloPadrao=ciclo||intervaloAval().fim,prazo=hoje();$('rhDevTitulo').textContent=isPdi?'Criar PDI':'Registrar feedback';$('rhDevNota').textContent=`Conectado ao ciclo ${cicloPadrao}. ${isPdi?'Defina objetivo, próximo passo e prazo.':'Registre uma devolutiva objetiva e acionável.'}`;$('rhDevCampos').innerHTML=`<input id="rhDevTipo" type="hidden" value="${tipo}"><div class="campo"><label for="rhDevCiclo">Ciclo de desempenho</label><input id="rhDevCiclo" type="month" required value="${esc(cicloPadrao)}"></div><div class="campo"><label for="rhDevPessoa">Colaborador</label><select id="rhDevPessoa" required>${opcoes(avaliadoId)}</select></div><div class="campo"><label for="rhDevPrazo">${isPdi?'Prazo do próximo passo':'Data do feedback'}</label><input id="rhDevPrazo" type="date" required value="${esc(prazo)}"></div><div class="campo campo-span-3"><label for="rhDevAssunto">${isPdi?'Objetivo do PDI':'Assunto do feedback'}</label><input id="rhDevAssunto" maxlength="100" required value="${esc(sugestao)}"></div><div class="campo campo-span-3"><label for="rhDevDescricao">${isPdi?'Próximos passos / critério de evolução':'Registro do feedback'}</label><textarea id="rhDevDescricao" maxlength="900" rows="4" required></textarea></div>${isPdi?`<div class="campo"><label for="rhDevSituacao">Situação</label><select id="rhDevSituacao"><option value="PLANEJADA">Planejado</option><option value="EM_ANDAMENTO">Em andamento</option><option value="CONCLUIDA">Concluído</option><option value="CANCELADA">Cancelado</option></select></div>`:'<input id="rhDevSituacao" type="hidden" value="CONCLUIDA">'}`;msg($('rhDevMsg'),'');$('rhDevBox').classList.remove('hidden');$('rhDevBox').scrollIntoView({behavior:'smooth',block:'start'})}
async function salvarDesenvolvimento(e){e.preventDefault();if(busy)return;try{const tipo=$('rhDevTipo').value,ciclo=$('rhDevCiclo').value,pessoaId=$('rhDevPessoa').value,prazo=$('rhDevPrazo').value,assunto=$('rhDevAssunto').value.trim(),descricao=$('rhDevDescricao').value.trim(),situacao=$('rhDevSituacao').value,p=colaborador(pessoaId);if(!['FEEDBACK','PDI'].includes(tipo)||!/^\d{4}-\d{2}$/.test(ciclo)||!p||p.empresaId!==empresaUnicaSelecionadaId()||!/^\d{4}-\d{2}-\d{2}$/.test(prazo)||!assunto||!descricao)throw Error('Revise ciclo, colaborador, data e descrição.');busy=true;msg($('rhDevMsg'),'Salvando...');const competencia=prazo.slice(0,7),titulo=`${prefixo(tipo,ciclo)} ${assunto}`,participanteIds=[pessoaId],participanteUsuarios=p.usuarioId?[p.usuarioId]:[];await criarDocumento('rhAcoes',{empresaId:empresaUnicaSelecionadaId(),competencia,prazo,tipo:'MELHORIA',titulo,descricao,situacao,status:'ativo',participanteIds,participanteUsuarios,registradoPor:state.usuario.id,origem:'sig'});$('rhDevBox').classList.add('hidden');emitirAlteracao('rh')}catch(err){console.error(err);msg($('rhDevMsg'),err.message||'Não foi possível salvar.')}finally{busy=false}setTimeout(async()=>{await carregar();trocarAbaAval($('rhDevTipo')?.value==='PDI'?'pdis':'feedbacks')},120)}

function classe9(valor){return valor>=4?'alto':valor>=3?'medio':'baixo'}
function dados9Box(avaliacoes){const por=new Map();for(const x of avaliacoes){const a=por.get(x.avaliadoId)||[];a.push(x);por.set(x.avaliadoId,a)}return[...por.entries()].map(([id,arr])=>({id,nome:pessoa(id),setor:colaborador(id)?.setor||'—',performance:arr.reduce((n,x)=>n+mediaNotas(x),0)/arr.length,potencial:arr.reduce((n,x)=>n+potencialNotas(x),0)/arr.length,qtde:arr.length}))}
function progressoCiclos(avaliacoes){const mapa=new Map();for(const x of avaliacoes){const k=`${x.ciclo}|${x.avaliadoId}`,item=mapa.get(k)||{ciclo:x.ciclo,avaliadoId:x.avaliadoId,modelo:modelo(x),papeis:new Map()};item.modelo=modelo(x);item.papeis.set(x.papel,(item.papeis.get(x.papel)||0)+1);mapa.set(k,item)}return[...mapa.values()].map(x=>{const req=MODELOS[x.modelo]?.papeis||MODELOS['360'].papeis,concluidos=req.filter(p=>x.papeis.has(p)).length;return{...x,req,concluidos,pct:Math.round(concluidos/req.length*100)}}).sort((a,b)=>String(b.ciclo).localeCompare(String(a.ciclo)))}
function trocarAbaAval(k){if(!ABAS_AV.includes(k))k='avaliacoes';abaAv=k;document.querySelectorAll('#pagina-rh-avaliacoes [data-rh-av-tab]').forEach(b=>b.classList.toggle('ativo',b.dataset.rhAvTab===k));$('rhCultBox-avaliacoes').classList.add('hidden');$('rhDevBox').classList.add('hidden');renderAvaliacoes()}
function renderAvaliacoes(){
  if(!$('rhCultConteudo-avaliacoes'))return;
  const p=intervaloAval(),f=filtrosAval();
  const av=registros.avaliacoes.filter(x=>x.status==='ativo'&&dentroPeriodoAvaliacao(x,p)&&pessoaPassa(x.avaliadoId,f)).sort((a,b)=>String(b.ciclo).localeCompare(String(a.ciclo)));
  const dev=registros.acoes.filter(x=>x.status!=='estornado'&&['FEEDBACK','PDI'].includes(tipoDesenvolvimento(x))&&pessoaPassa((x.participanteIds||[])[0],f));
  const feedbacks=dev.filter(x=>tipoDesenvolvimento(x)==='FEEDBACK'&&dentroPeriodoAcao(x,p));
  const pdis=dev.filter(x=>tipoDesenvolvimento(x)==='PDI'&&cicloAcao(x)>=p.inicio&&cicloAcao(x)<=p.fim);
  const ativos=av,avaliados=new Set(ativos.map(x=>x.avaliadoId)),box=dados9Box(ativos),media=box.length?box.reduce((n,x)=>n+x.performance,0)/box.length:0,ciclos=progressoCiclos(ativos);
  $('rhCultResumo-avaliacoes').innerHTML=`<div class="kpi-card"><span>Avaliações</span><strong>${ativos.length}</strong><small>${avaliados.size} colaborador(es)</small></div><div class="kpi-card"><span>Ciclos concluídos</span><strong>${ciclos.filter(x=>x.pct===100).length}</strong><small>${ciclos.length} ciclo(s) acompanhado(s)</small></div><div class="kpi-card"><span>PDIs ativos</span><strong>${pdis.filter(x=>x.situacao!=='CONCLUIDA'&&x.situacao!=='CANCELADA').length}</strong><small>${feedbacks.length} feedback(s)</small></div><div class="kpi-card"><span>Média de desempenho</span><strong>${box.length?media.toFixed(1):'—'}</strong><small>escala de 1 a 5</small></div>`;
  const acoes=$('rhCultAcoesLista');acoes.innerHTML='';
  if(abaAv==='avaliacoes'){$('rhCultTituloLista').textContent='Avaliações de desempenho';$('rhCultNotaLista').textContent=`Período global: ${p.label} ${p.ano} · modelos 90°, 180°, 360° e 540°.`;if(perm('acoes','lancar'))acoes.innerHTML='<button class="btn-secundario" type="button" data-novo-feedback>+ Feedback</button><button class="btn-secundario" type="button" data-novo-pdi>+ PDI</button>';const progresso=ciclos.length?`<div class="production-kpis">${ciclos.slice(0,8).map(c=>`<div class="kpi-card"><span>${esc(pessoa(c.avaliadoId))} · ${esc(c.ciclo)} · ${MODELOS[c.modelo]?.rotulo||'360°'}</span><strong>${c.pct}%</strong><small>${c.req.map(r=>`${r}: ${c.papeis.get(r)||0}`).join(' · ')}</small></div>`).join('')}</div>`:'';$('rhCultConteudo-avaliacoes').innerHTML=progresso+`<div class="tabela-container"><table class="tabela"><thead><tr><th>Ciclo</th><th>Modelo</th><th>Avaliado</th><th>Avaliador</th><th>Perspectiva</th><th>Média</th><th>Evidências</th><th>Ações</th></tr></thead><tbody>${av.map(x=>`<tr><td>${esc(x.ciclo)}</td><td>${MODELOS[modelo(x)]?.rotulo||'360°'}</td><td>${esc(pessoa(x.avaliadoId))}</td><td>${esc(nomeAvaliador(x))}</td><td>${esc(x.papel)}</td><td><strong>${mediaNotas(x).toFixed(1)}</strong></td><td>${esc(x.comentario||'—')}</td><td class="acoes-tabela">${perm('avaliacoes','editar')?`<button type="button" class="btn-acao destaque" data-av-edit="${esc(x.id)}">Editar</button>`:''}${perm('acoes','lancar')?`<button type="button" class="btn-acao" data-av-feedback="${esc(x.id)}">Feedback</button><button type="button" class="btn-acao" data-av-pdi="${esc(x.id)}">Criar PDI</button>`:''}${admin()?`<button type="button" class="btn-acao perigo" data-av-estorno="${esc(x.id)}">Estornar ADM</button>`:''}</td></tr>`).join('')||'<tr><td colspan="8">Nenhuma avaliação no período.</td></tr>'}</tbody></table></div>`}
  else if(abaAv==='feedbacks'){$('rhCultTituloLista').textContent='Feedbacks registrados';$('rhCultNotaLista').textContent=`Período global: ${p.label} ${p.ano}.`;if(perm('acoes','lancar'))acoes.innerHTML='<button class="btn-primario" type="button" data-novo-feedback>+ Registrar feedback</button>';$('rhCultConteudo-avaliacoes').innerHTML=`<div class="tabela-container"><table class="tabela"><thead><tr><th>Data</th><th>Ciclo</th><th>Colaborador</th><th>Assunto</th><th>Registro</th></tr></thead><tbody>${feedbacks.sort((a,b)=>String(b.prazo).localeCompare(String(a.prazo))).map(x=>`<tr><td>${dataBR(x.prazo)}</td><td>${esc(cicloAcao(x))}</td><td>${esc(pessoa((x.participanteIds||[])[0]))}</td><td>${esc(tituloLimpo(x))}</td><td>${esc(x.descricao||'—')}</td></tr>`).join('')||'<tr><td colspan="5">Nenhum feedback no período.</td></tr>'}</tbody></table></div>`}
  else if(abaAv==='pdis'){$('rhCultTituloLista').textContent='Planos de Desenvolvimento Individual';$('rhCultNotaLista').textContent=`Período global: ${p.label} ${p.ano}.`;if(perm('acoes','lancar'))acoes.innerHTML='<button class="btn-primario" type="button" data-novo-pdi>+ Criar PDI</button>';$('rhCultConteudo-avaliacoes').innerHTML=`<div class="tabela-container"><table class="tabela"><thead><tr><th>Ciclo origem</th><th>Colaborador</th><th>Objetivo</th><th>Próximo passo</th><th>Prazo</th><th>Situação</th></tr></thead><tbody>${pdis.sort((a,b)=>String(b.prazo).localeCompare(String(a.prazo))).map(x=>`<tr><td>${esc(cicloAcao(x))}</td><td>${esc(pessoa((x.participanteIds||[])[0]))}</td><td>${esc(tituloLimpo(x))}</td><td>${esc(x.descricao||'—')}</td><td>${dataBR(x.prazo)}</td><td>${esc(x.situacao||'PLANEJADA')}</td></tr>`).join('')||'<tr><td colspan="6">Nenhum PDI conectado aos ciclos do período.</td></tr>`}
  else if(abaAv==='calibracao'){$('rhCultTituloLista').textContent='Calibração estruturada · Matriz 9-box';$('rhCultNotaLista').textContent=`Período global: ${p.label} ${p.ano} · Performance = média geral. Potencial = média de Iniciativa + Liderança.`;const grupos={alto:{alto:[],medio:[],baixo:[]},medio:{alto:[],medio:[],baixo:[]},baixo:{alto:[],medio:[],baixo:[]}};for(const x of box)grupos[classe9(x.potencial)][classe9(x.performance)].push(x);const cel=(pot,perf,titulo)=>`<article class="rh-nine-cell"><div class="rh-nine-title"><strong>${titulo}</strong><span>${grupos[pot][perf].length}</span></div>${grupos[pot][perf].map(x=>`<button type="button" class="rh-nine-person" data-nine-person="${esc(x.id)}"><strong>${esc(x.nome)}</strong><small>${esc(x.setor)} · D ${x.performance.toFixed(1)} / P ${x.potencial.toFixed(1)}</small></button>`).join('')||'<div class="rh-empty">Sem colaboradores</div>'}</article>`;$('rhCultConteudo-avaliacoes').innerHTML=`<div class="rh-nine-wrapper"><div class="rh-nine-y">Potencial ↑</div><div class="rh-nine-grid">${cel('alto','baixo','Aposta futura')}${cel('alto','medio','Alto potencial')}${cel('alto','alto','Talento-chave')}${cel('medio','baixo','Potencial a desenvolver')}${cel('medio','medio','Desempenho sólido')}${cel('medio','alto','Forte desempenho')}${cel('baixo','baixo','Reavaliar aderência')}${cel('baixo','medio','Especialista consistente')}${cel('baixo','alto','Referência técnica')}</div><div class="rh-nine-x">Desempenho →</div></div>`}
  else{$('rhCultTituloLista').textContent='Histórico centralizado';$('rhCultNotaLista').textContent=`Período global: ${p.label} ${p.ano} · somente registros ativos.`;const hist=[...ativos.map(x=>({data:`${x.ciclo}-01`,tipo:'AVALIAÇÃO',ciclo:x.ciclo,pessoaId:x.avaliadoId,titulo:`${MODELOS[modelo(x)]?.rotulo||'360°'} · ${x.papel} · ${nomeAvaliador(x)} · média ${mediaNotas(x).toFixed(1)}`,detalhe:x.comentario||'Sem comentário'})),...feedbacks.map(x=>({data:x.prazo,tipo:'FEEDBACK',ciclo:cicloAcao(x),pessoaId:(x.participanteIds||[])[0],titulo:tituloLimpo(x),detalhe:x.descricao||''})),...pdis.map(x=>({data:x.prazo,tipo:'PDI',ciclo:cicloAcao(x),pessoaId:(x.participanteIds||[])[0],titulo:tituloLimpo(x),detalhe:`${x.descricao||''} · ${x.situacao||'PLANEJADA'}`}))].sort((a,b)=>String(b.data).localeCompare(String(a.data)));$('rhCultConteudo-avaliacoes').innerHTML=`<div class="rh-history">${hist.map(x=>`<article class="rh-history-item"><div class="rh-history-date">${dataBR(x.data)}</div><div><span class="rh-history-type">${esc(x.tipo)}</span><strong>${esc(pessoa(x.pessoaId))}</strong><small>Ciclo ${esc(x.ciclo)}</small><p>${esc(x.titulo)}</p><small>${esc(x.detalhe)}</small></div></article>`).join('')||'<div class="rh-empty">Nenhum registro no período.</div>'}</div>`}
  bindAvaliacoes();
}
function bindAvaliacoes(){document.querySelectorAll('#pagina-rh-avaliacoes [data-av-edit]').forEach(b=>b.addEventListener('click',()=>abrirAvaliacao(registros.avaliacoes.find(x=>x.id===b.dataset.avEdit))));document.querySelectorAll('#pagina-rh-avaliacoes [data-av-feedback]').forEach(b=>b.addEventListener('click',()=>{const x=registros.avaliacoes.find(v=>v.id===b.dataset.avFeedback);if(x)abrirDesenvolvimento('FEEDBACK',x.avaliadoId,x.ciclo)}));document.querySelectorAll('#pagina-rh-avaliacoes [data-av-pdi]').forEach(b=>b.addEventListener('click',()=>{const x=registros.avaliacoes.find(v=>v.id===b.dataset.avPdi);if(x)abrirDesenvolvimento('PDI',x.avaliadoId,x.ciclo)}));document.querySelectorAll('#pagina-rh-avaliacoes [data-novo-feedback]').forEach(b=>b.addEventListener('click',()=>abrirDesenvolvimento('FEEDBACK')));document.querySelectorAll('#pagina-rh-avaliacoes [data-novo-pdi]').forEach(b=>b.addEventListener('click',()=>abrirDesenvolvimento('PDI')));document.querySelectorAll('#pagina-rh-avaliacoes [data-av-estorno]').forEach(b=>b.addEventListener('click',()=>estornar(b.dataset.avEstorno)));document.querySelectorAll('#pagina-rh-avaliacoes [data-nine-person]').forEach(b=>b.addEventListener('click',()=>{$('rhCultPessoa').value=b.dataset.ninePerson;trocarAbaAval('historico')}))}
async function estornar(id){if(!admin())return;const x=registros.avaliacoes.find(v=>v.id===id);if(!x||x.status!=='ativo')return;const ok=await confirmarAcaoAdministrativa({titulo:'Estornar avaliação',descricao:'O registro será retirado dos indicadores e do histórico centralizado, permanecendo apenas para auditoria administrativa.',motivoLabel:'Motivo obrigatório',confirmarTexto:'Estornar',perigosa:true});if(!ok)return;try{await atualizarComAuditoria({colecao:'rhAvaliacoes360',id,empresaId:x.empresaId,modulo:'rhAvaliacoes360',acao:'estorno',motivo:ok.motivo,resumo:'Estorno Avaliações/feedback',snapshotAntes:x,alteracoes:{status:'estornado',motivoEstorno:ok.motivo,estornadoPor:state.usuario.id,estornadoEm:new Date().toISOString()}});emitirAlteracao('rh');setTimeout(()=>carregar(),120)}catch(e){console.error(e);alert('Não foi possível estornar.')}}
async function carregar(){if(busy||!acesso()||!podeVer('avaliacoes'))return;busy=true;try{const req=[listarDocumentos('rhColaboradores'),listarDocumentos('rhAvaliacoes360')];if(podeVer('acoes'))req.push(listarDocumentos('rhAcoes'));const r=await Promise.all(req);pessoas=r[0];registros.avaliacoes=r[1];registros.acoes=r[2]||[];atualizarFiltrosAval();renderAvaliacoes();$('rhCultAviso-avaliacoes')?.classList.add('hidden')}catch(e){console.error(e);if($('rhCultAviso-avaliacoes')){$('rhCultAviso-avaliacoes').textContent='Não foi possível carregar avaliações. Confira perfil e Rules.';$('rhCultAviso-avaliacoes').classList.remove('hidden')}}finally{busy=false}}
function instalar(){montar();menu()}
instalar();
window.addEventListener('sig:ready',instalar);
window.addEventListener('sig:contexto-changed',()=>{if(!$('pagina-rh-avaliacoes')?.classList.contains('hidden'))carregar()});
window.addEventListener('sig:data-changed',e=>{if(e.detail?.modulo==='rh'&&!$('pagina-rh-avaliacoes')?.classList.contains('hidden'))setTimeout(()=>carregar(),120)});